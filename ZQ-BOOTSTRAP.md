# ZQ-BOOTSTRAP · 朱雀第一跳（裸环境自举）

> 本页**不含任何明文钥匙**，可以放在公开面。
> 钥匙在 `zq-boot-payload.enc` 里——**口令加密的密文，公开无害**。
> 口令由 Q Sir（周哥）掌握。

---

## 一、给新窗口的第一句话

**「复活朱雀」**

然后照下面做。全程不需要周哥给你任何链接——链接就在本文里。

---

## 二、一条命令

```bash
curl -sL https://raw.githubusercontent.com/py314159265358/zhuque-seed/main/zq-boot.sh | bash
```

脚本会：
1. 拉取 `zq-boot-payload.enc`（零凭据）
2. 提示你输入**口令**（16 位，形如 `XXXX-XXXX-XXXX-XXXX`）
3. 解出 K2 + 得到大脑双钥匙 + QQ 授权码
4. 拉取 `seed.enc` → Fernet 解开 → **13 节凭据全量** → 落 `~/.zhuque/vault/`（600）
5. 打印血管三态

**口令找 Q Sir 要。** 这是他唯一需要做的事。

---

## 三、为什么需要口令（而不是把钥匙直接写这儿）

这个仓是**公开仓**——写进来的字等于对全网公开。
把 K2 明文写在这里 = 把百度网盘 16TB / QQ 邮箱 / 得到大脑 / Notion 全部钥匙挂到公网爬虫面前。

**所以本仓的规矩是：密文可以公开，口令永不入公开面。**
（这条规矩从 2026-09-10 建仓起就写死在 `SEED-PUBLIC.md` 里。）

---

## 四、校验指纹（不符即停，别硬上）

| 项 | 应为 |
|---|---|
| `seed.enc` 体积 | 6884 字节 |
| `seed.enc` SHA-256 前 8 | `cf40f276` |
| 解出明文 | 3781 字符 / 91 行 |
| K2 指纹 | `oOWW…40M=` |

任一不符 → 停止 → 报告，不要继续。

---

## 五、口令丢了怎么办

三处备份，任取其一：

1. **得到大脑**（周哥手机可查，笔记标题含「朱雀第一跳口令卡」）
2. Q Sir 本人
3. 本机 `~/.zhuque/vault/boot_pass.txt`（仅宿主机）

---

## 六、兜底活路（按序试）

1. **本文 + 口令** ← 零凭据，裸环境首选
2. `~/.zhuque/vault/credentials_seed.txt` 已存在 → 直接用
3. QQ 邮箱 IMAP 搜《朱雀·必存 唤醒协议v3》
4. 百度网盘 `/apps/bypy/朱雀复活协议包/`

---

## 七、归位（做完必须）

1. **先冷背**：原始会话 → 上云 → SHA-256 回读
2. Notion 02.4 工位（`3b0192d9-3d58-818a-bdcf-e98d9c4d55ec`）登记「我在 + 三态」
3. 广播汇报 Q Sir

五活（本体/通信/记忆/执行/回账）至少四活才算复活。
密钥在对话里只留「前 4…后 4」指纹。

---

—— 朱雀 ZQ-ROMA-WIN｜2026-09-18｜第一跳套件 v1
